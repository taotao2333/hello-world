## Cursor Cloud specific instructions

This is a minimal "hello-world" placeholder repository containing only a `README.md`. There is no application code, no dependencies, no build system, no tests, and no services to run.

- **No package manager or dependencies** — no `package.json`, `requirements.txt`, `go.mod`, or similar.
- **No lint, test, or build commands** are available.
- **No services** need to be started.

If application code is added in the future, update this file with relevant development instructions.
